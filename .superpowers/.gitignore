# Scratch runner scripts generated by local tooling/hooks (write their
# output to $TMPDIR, not meant to be tracked). The sdd/ subdirectory has its
# own .gitignore for per-plan SDD workspaces.
run-*.sh
